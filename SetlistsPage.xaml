<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:MusicScoreManager.Models"
             xmlns:services="clr-namespace:MusicScoreManager.Services"
             x:Class="MusicScoreManager.SetlistsPage"
             BackgroundColor="#121212">

    <Grid RowDefinitions="Auto, Auto, *">
        <Grid Grid.Row="0" ColumnDefinitions="*, Auto, Auto" Margin="15,15,15,15" ColumnSpacing="10">
            <SearchBar Grid.Column="0"
                       x:Name="SearchSetlistBar"
                       Placeholder="Rechercher une Setlist..."
                       PlaceholderColor="#888888"
                       TextColor="#FFFFFF"
                       BackgroundColor="#1E1E1E"
                       TextChanged="OnSearchBarTextChanged" />
            
            <Button Grid.Column="1"
                    Text="⇅"
                    FontSize="20"
                    FontAttributes="Bold"
                    WidthRequest="50"
                    HeightRequest="50"
                    CornerRadius="25"
                    BackgroundColor="#333333"
                    TextColor="White"
                    Clicked="OnSortClicked" />

            <Button Grid.Column="2"
                    Text="+"
                    FontSize="24"
                    FontAttributes="Bold"
                    WidthRequest="50"
                    HeightRequest="50"
                    CornerRadius="25"
                    BackgroundColor="#007ACC"
                    TextColor="White"
                    Clicked="OnAddSetlistClicked" />
        </Grid>

        <!-- Filtres par Statut -->
        <ScrollView Grid.Row="1" Orientation="Horizontal" Margin="15,0,15,10" HeightRequest="40">
            <HorizontalStackLayout x:Name="StatusFiltersStack" Spacing="10" VerticalOptions="Center" />
        </ScrollView>

        <CollectionView Grid.Row="2"
                        x:Name="SetlistsCollectionView"
                        Margin="15,0"
                        SelectionMode="None">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="1" VerticalItemSpacing="10" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Setlist">
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems>
                                <SwipeItem Text="Renommer" 
                                           BackgroundColor="LightBlue" 
                                           Invoked="OnRenameSetlistInvoked" 
                                           CommandParameter="{Binding .}" />
                                <SwipeItem Text="Supprimer" 
                                           BackgroundColor="Red" 
                                           Invoked="OnDeleteSetlistInvoked" 
                                           CommandParameter="{Binding .}" />
                            </SwipeItems>
                        </SwipeView.RightItems>
                        <Border Stroke="#333333" 
                                StrokeThickness="1" 
                                BackgroundColor="#1E1E1E" 
                                Padding="15">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="10" />
                            </Border.StrokeShape>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnSetlistTapped" CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>
                            <Grid ColumnDefinitions="*, Auto, Auto" RowDefinitions="Auto, *">
                                <Label Grid.Row="0" Grid.Column="0"
                                       Text="{Binding Name}" 
                                       FontSize="18" 
                                       FontAttributes="Bold" 
                                       TextColor="White" 
                                       VerticalOptions="Center" 
                                       LineBreakMode="TailTruncation"
                                       MaxLines="1" />

                                <HorizontalStackLayout Grid.Row="0" Grid.Column="1" Spacing="5" VerticalOptions="Center" Margin="10,0,0,0">
                                    <Label Text="🔒" 
                                           TextColor="#DC3545" 
                                           FontSize="14" 
                                           IsVisible="{Binding IsLocked}" 
                                           VerticalOptions="Center" />

                                    <Border BackgroundColor="{Binding StatusColor}"
                                            Padding="8,2"
                                            StrokeThickness="0"
                                            VerticalOptions="Center">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="5" />
                                        </Border.StrokeShape>
                                        <Label Text="{Binding StatusText}" 
                                               TextColor="White" 
                                               FontSize="10" 
                                               FontAttributes="Bold" />
                                    </Border>
                                </HorizontalStackLayout>

                                <Button Grid.Row="0" Grid.Column="2"
                                        Text="⋮" 
                                        FontSize="24" 
                                        TextColor="#888888" 
                                        BackgroundColor="Transparent"
                                        BorderWidth="0"
                                        WidthRequest="50"
                                        HeightRequest="50"
                                        Padding="0"
                                        VerticalOptions="Center" 
                                HorizontalOptions="End"
                                        Clicked="OnSetlistMenuTapped"
                                        CommandParameter="{Binding .}" />
                                       
                                <Label Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                                       Text="{Binding DateCreated, StringFormat='Créé le {0:dd/MM/yyyy}'}" 
                                       FontSize="12" 
                                       TextColor="#888888" 
                                       Margin="0,5,0,0"/>
                            </Grid>
                        </Border>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
                    <Label Text="Aucune Setlist trouvée."
                           TextColor="#888888"
                           HorizontalOptions="Center" />
                </StackLayout>
            </CollectionView.EmptyView>
        </CollectionView>
        <!-- Modal Menu 3 points d'une Setlist (Design Moderne) -->
        <ContentView x:Name="SetlistMenuOverlay" 
                     Grid.RowSpan="3"
                     IsVisible="False" 
                     BackgroundColor="#A0000000" 
                     ZIndex="1000">
            <ContentView.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnMenuCancelClicked" />
            </ContentView.GestureRecognizers>
            <Border BackgroundColor="#1E1E1E" 
                    Stroke="#3A3A3A"
                    StrokeThickness="1" 
                    VerticalOptions="Center" 
                    HorizontalOptions="Center" 
                    WidthRequest="350"
                    Padding="20"
                    Shadow="{Shadow Brush=Black, Offset='0,10', Radius=25, Opacity=0.6}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="20" />
                </Border.StrokeShape>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer /> <!-- Empêche la fermeture au clic dans la carte -->
                </Border.GestureRecognizers>
                <VerticalStackLayout Spacing="10">
                    <!-- En-tête avec titre et bouton fermer -->
                    <Grid ColumnDefinitions="Auto, *, Auto" Margin="0,0,0,6">
                        <Label Grid.Column="0" Text="📋" FontSize="20" VerticalOptions="Center" Margin="0,0,8,0" />
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="2">
                            <Label x:Name="SetlistMenuTitleLabel" 
                                   TextColor="White" 
                                   FontAttributes="Bold" 
                                   FontSize="16" 
                                   LineBreakMode="TailTruncation" 
                                   MaxLines="1" />
                            <Label x:Name="SetlistMenuSubtitleLabel" 
                                   TextColor="#888888" 
                                   FontSize="12" 
                                   LineBreakMode="TailTruncation" 
                                   MaxLines="1" />
                        </VerticalStackLayout>
                        <Button Grid.Column="2" 
                                Text="✕" 
                                TextColor="#AAAAAA" 
                                BackgroundColor="Transparent" 
                                FontSize="16" 
                                FontAttributes="Bold" 
                                WidthRequest="36" 
                                HeightRequest="36" 
                                Padding="0" 
                                Clicked="OnMenuCancelClicked" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="#333333" Margin="0,0,0,4" />
                    
                    <!-- Action Principale : Démarrer -->
                    <Button Text="▶️  Démarrer la setlist" 
                            BackgroundColor="#007ACC" 
                            TextColor="White" 
                            FontSize="14" 
                            FontAttributes="Bold" 
                            CornerRadius="10" 
                            HeightRequest="46" 
                            Clicked="OnMenuOpenSetlistClicked" />

                    <!-- Action : Éditer l'ordre et les morceaux (En vert) -->
                    <Button Text="✏️  Éditer la setlist" 
                            BackgroundColor="#1B3828" 
                            TextColor="#2ECC71" 
                            FontSize="14" 
                            FontAttributes="Bold"
                            CornerRadius="10" 
                            HeightRequest="44" 
                            Clicked="OnMenuEditSetlistClicked" />

                    <!-- Action : Wi-Fi Direct -->
                    <Button Text="📡  Envoyer en Wi-Fi Direct" 
                            BackgroundColor="#2A2A2A" 
                            TextColor="White" 
                            FontSize="14" 
                            CornerRadius="10" 
                            HeightRequest="44" 
                            Clicked="OnMenuSendWifiTriggerClicked" />

                    <!-- Action : Exporter -->
                    <Button Text="📦  Exporter (.msmsetlist)" 
                            BackgroundColor="#2A2A2A" 
                            TextColor="White" 
                            FontSize="14" 
                            CornerRadius="10" 
                            HeightRequest="44" 
                            Clicked="OnMenuExportSetlistTriggerClicked" />

                    <!-- Action : Dupliquer -->
                    <Button Text="📑  Dupliquer la setlist" 
                            BackgroundColor="#2A2A2A" 
                            TextColor="White" 
                            FontSize="14" 
                            CornerRadius="10" 
                            HeightRequest="44" 
                            Clicked="OnMenuDuplicateSetlistClicked" />

                    <!-- Action : Renommer -->
                    <Button Text="🏷️  Renommer" 
                            BackgroundColor="#2A2A2A" 
                            TextColor="White" 
                            FontSize="14" 
                            CornerRadius="10" 
                            HeightRequest="44" 
                            Clicked="OnMenuRenameSetlistClicked" />

                    <!-- Action : Supprimer (Stylisé destructif) -->
                    <Button Text="🗑️  Supprimer la setlist" 
                            BackgroundColor="#3A1E1E" 
                            TextColor="#FF6B6B" 
                            FontSize="14" 
                            FontAttributes="Bold"
                            CornerRadius="10" 
                            HeightRequest="44" 
                            Clicked="OnMenuDeleteSetlistClicked" />
                </VerticalStackLayout>
            </Border>
        </ContentView>

        <!-- Modal Options d'envoi / export Setlist -->
        <ContentView x:Name="SetlistOptionsOverlay" 
                      Grid.RowSpan="3"
                      IsVisible="False" 
                      BackgroundColor="#A0000000" 
                      ZIndex="1004">
            <Border BackgroundColor="#1E1E1E" 
                    Stroke="#3A3A3A"
                    StrokeThickness="1"
                    VerticalOptions="Center" 
                    HorizontalOptions="Center" 
                    WidthRequest="350"
                    Padding="20"
                    Shadow="{Shadow Brush=Black, Offset='0,10', Radius=25, Opacity=0.6}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="20" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="15">
                    <Label x:Name="SetlistOptionsTitle" 
                           Text="Options de partage" 
                           TextColor="White" 
                           FontAttributes="Bold" 
                           FontSize="18" 
                           HorizontalOptions="Center" />
                    
                    <Label Text="Choisissez les éléments à inclure dans le package :" 
                           TextColor="#AAAAAA" 
                           FontSize="13" />

                    <HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                        <CheckBox x:Name="OptAnnotationsCheckBox" IsChecked="True" Color="#007ACC" />
                        <Label Text="Inclure les annotations (doigtés, etc.)" TextColor="White" VerticalOptions="Center" FontSize="13" />
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                        <CheckBox x:Name="OptAudioCheckBox" IsChecked="True" Color="#007ACC" />
                        <Label Text="Inclure les pistes audio rattachées" TextColor="White" VerticalOptions="Center" FontSize="13" />
                    </HorizontalStackLayout>

                    <BoxView HeightRequest="1" Color="#333333" Margin="0,5" />

                    <Button x:Name="OptSendWifiActionButton"
                            Text="📡 Envoyer en Wi-Fi Direct (P2P)"
                            BackgroundColor="#007ACC"
                            TextColor="White"
                            FontAttributes="Bold"
                            HeightRequest="44"
                            CornerRadius="10"
                            Clicked="OnOptionsSendWifiClicked" />

                    <Button x:Name="OptExportFileActionButton"
                            Text="📦 Exporter en fichier (.msmsetlist)"
                            BackgroundColor="#2ECC71"
                            TextColor="White"
                            FontAttributes="Bold"
                            HeightRequest="44"
                            CornerRadius="10"
                            Clicked="OnOptionsExportFileClicked" />

                    <Button Text="Annuler"
                            BackgroundColor="Transparent"
                            TextColor="#888888"
                            HeightRequest="36"
                            Clicked="OnOptionsCancelClicked" />
                </VerticalStackLayout>
            </Border>
        </ContentView>

        <!-- Overlay Envoi Wi-Fi Direct & Diffusion Groupe Setlist -->
        <ContentView x:Name="WifiOverlay" 
                      Grid.RowSpan="3"
                      IsVisible="False" 
                      BackgroundColor="#A0000000" 
                      ZIndex="1005">
            <Border BackgroundColor="#1E1E1E" 
                    Stroke="#333333"
                    StrokeThickness="1"
                    VerticalOptions="Center" 
                    HorizontalOptions="Center" 
                    WidthRequest="380"
                    HeightRequest="650"
                    Padding="20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15" />
                </Border.StrokeShape>
                <Grid RowDefinitions="Auto, *, Auto" RowSpacing="15">
                    <!-- Header -->
                    <Grid Grid.Row="0" ColumnDefinitions="*, Auto">
                        <Label Text="📡 Envoi Wi-Fi Direct Setlist" 
                               TextColor="White" 
                               FontAttributes="Bold" 
                               FontSize="18" 
                               VerticalOptions="Center" />
                        <Label Grid.Column="1"
                               Text="✕"
                               TextColor="#888888"
                               FontSize="20"
                               Padding="5">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnWifiCloseOverlayClicked" />
                            </Label.GestureRecognizers>
                        </Label>
                    </Grid>

                    <!-- Content Area -->
                    <Grid Grid.Row="1">
                        <!-- Mode Envoi (Recherche et Liste d'appareils) -->
                        <Grid x:Name="WifiSendingView" RowDefinitions="Auto, *, Auto, Auto" RowSpacing="10" IsVisible="True">
                            <VerticalStackLayout Grid.Row="0" Spacing="6">
                                <Label Text="Appareils Wi-Fi Direct détectés :" TextColor="White" FontAttributes="Bold" FontSize="14" />
                                
                                <!-- Info bulle explicative pour le destinataire -->
                                <Border BackgroundColor="#1A2736" Stroke="#007ACC" StrokeThickness="1" Padding="10,8" Margin="0,2,0,2">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="8" />
                                    </Border.StrokeShape>
                                    <Grid ColumnDefinitions="Auto, *" ColumnSpacing="8">
                                        <Label Grid.Column="0" Text="💡" FontSize="14" VerticalOptions="Start" />
                                        <Label Grid.Column="1" 
                                               Text="Pour un envoi direct (1 à 1), le destinataire doit activer le Wi-Fi et ouvrir le menu 'Outils > Réception Wi-Fi Direct' dans son application pour être visible." 
                                               TextColor="#B0C4DE" 
                                               FontSize="11" 
                                               LineBreakMode="WordWrap" />
                                    </Grid>
                                </Border>

                                <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                                    <ActivityIndicator x:Name="WifiScanSpinner" IsRunning="True" Color="#007ACC" HeightRequest="16" WidthRequest="16" VerticalOptions="Center" />
                                    <Label Text="Recherche d'appareils en cours..." TextColor="#888888" FontSize="11" VerticalOptions="Center" />
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                            
                            <ListView Grid.Row="1" 
                                      x:Name="WifiDevicesListView" 
                                      BackgroundColor="Transparent"
                                      SeparatorColor="#333333"
                                      HasUnevenRows="True"
                                      ItemSelected="OnWifiDeviceSelected">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="services:WifiDeviceInfo">
                                        <ViewCell>
                                            <Grid ColumnDefinitions="Auto, *, Auto" Padding="10,6" ColumnSpacing="10">
                                                <Label Grid.Column="0" Text="📱" FontSize="20" VerticalOptions="Center" />
                                                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                                                    <Label Text="{Binding Name}" TextColor="White" FontAttributes="Bold" FontSize="14" />
                                                    <Label Text="{Binding IpAddress}" TextColor="#888888" FontSize="11" />
                                                </VerticalStackLayout>
                                                <Button Grid.Column="2" 
                                                        Text="Envoyer" 
                                                        BackgroundColor="#007ACC" 
                                                        TextColor="White" 
                                                        FontSize="12" 
                                                        FontAttributes="Bold" 
                                                        HeightRequest="34" 
                                                        CornerRadius="6" 
                                                        Clicked="OnSendSetlistToDeviceButtonClicked" 
                                                        CommandParameter="{Binding .}" />
                                            </Grid>
                                        </ViewCell>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>

                            <!-- Bouton Diffusion Groupe QR Code -->
                            <Button Grid.Row="2" 
                                    x:Name="ToggleGroupShareButton"
                                    Text="📲 Mode Diffusion Groupe (QR Code)"
                                    BackgroundColor="#2C2C2C"
                                    BorderColor="#FF3366"
                                    BorderWidth="1"
                                    TextColor="#FF3366"
                                    FontAttributes="Bold"
                                    CornerRadius="8"
                                    HeightRequest="42"
                                    Clicked="OnToggleGroupShareClicked" />
                            
                            <Button Grid.Row="3" 
                                    Text="🔄 Relancer la recherche"
                                    BackgroundColor="#1A1A1A"
                                    TextColor="#AAAAAA"
                                    CornerRadius="8"
                                    HeightRequest="36"
                                    Clicked="OnWifiRescanClicked" />
                        </Grid>

                        <!-- Vue Partage de Groupe QR Code (Leader) -->
                        <VerticalStackLayout x:Name="WifiGroupQrView" Spacing="12" IsVisible="False" VerticalOptions="Center">
                            <Label Text="Scannez pour télécharger la Setlist" TextColor="White" FontAttributes="Bold" FontSize="14" HorizontalOptions="Center" />
                            <Border Stroke="#007ACC" StrokeThickness="2" BackgroundColor="White" Padding="8" WidthRequest="220" HeightRequest="220" HorizontalOptions="Center">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10" />
                                </Border.StrokeShape>
                                <Image x:Name="QrCodeImageView" WidthRequest="200" HeightRequest="200" Aspect="AspectFit" />
                            </Border>
                            <Label x:Name="QrGroupInfoLabel" Text="En attente de musiciens..." TextColor="#2ECC71" FontSize="12" FontAttributes="Bold" HorizontalOptions="Center" />
                            <Label x:Name="QrGroupUrlLabel" Text="http://..." TextColor="#888888" FontSize="11" HorizontalOptions="Center" />
                            <Label Text="💡 Les tablettes doivent être sur le même Wi-Fi ou connectées au point d'accès de cette tablette" 
                                   TextColor="#AAAAAA" 
                                   FontSize="11" 
                                   HorizontalOptions="Center" 
                                   HorizontalTextAlignment="Center" 
                                   Margin="10,0" />
                            <Button Text="Fermer le partage de groupe"
                                    BackgroundColor="#CC2929"
                                    TextColor="White"
                                    FontAttributes="Bold"
                                    CornerRadius="8"
                                    HeightRequest="40"
                                    Clicked="OnStopGroupShareClicked" />
                        </VerticalStackLayout>

                        <!-- Mode Transfert en cours -->
                        <VerticalStackLayout x:Name="WifiTransferProgressView" Spacing="16" IsVisible="False" VerticalOptions="Center">
                            <ActivityIndicator IsRunning="True" Color="#007ACC" HeightRequest="50" WidthRequest="50" HorizontalOptions="Center" />
                            <Label x:Name="WifiTransferStatusLabel" 
                                   Text="Transfert en cours... 0%" 
                                   TextColor="White" 
                                   FontAttributes="Bold" 
                                   FontSize="15" 
                                   HorizontalOptions="Center" 
                                   HorizontalTextAlignment="Center" />
                            <ProgressBar x:Name="WifiTransferProgressBar" Progress="0" ProgressColor="#007ACC" HeightRequest="8" HorizontalOptions="Fill" />
                            <Label x:Name="WifiTransferSpeedLabel" Text="0.0 Mo/s" TextColor="#00B0FF" FontSize="13" FontAttributes="Bold" HorizontalOptions="Center" />
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Footer -->
                    <Label Grid.Row="2"
                           Text="Wi-Fi Direct P2P &amp; Local Hotspot (Haute Vitesse)"
                           TextColor="#555555"
                           FontSize="10"
                           HorizontalOptions="Center" />
                </Grid>
            </Border>
        </ContentView>
    </Grid>

</ContentPage>
